# Simple AudioSystem

Simple .wav playing abilities with miniaudio. It supports play, loop and fade-out of .wav files.

## Building

```bash
mkdir -p build
cd build
cmake ..
cd ..
cmake --build build
```
